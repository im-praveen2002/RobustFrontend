import React from 'react';
import ReactDOM from 'react-dom/client';

import Connect from './Connecting/Connect.jsx';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    < Connect/>
  </React.StrictMode>
);